version: '3.8'

services:
  mysql:
    image: mysql:8.0
    ports:
      - "3307:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=1234
      - MYSQL_DATABASE=hospitaldb

    restart: unless-stopped

    volumes:
      - clinic_db_data:/var/lib/mysql

    networks:
      - clinic_network
  

  app:
    build: .  #the Dockerfile in this directory
    ports:
      - "8080:8080"
    environment:                       #not localhost
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/hospitaldb?createDatabaseIfNotExist=false&serverTimezone=UTC
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=1234
    networks:
      - clinic_network
    depends_on:
      - mysql #Wait for mysql to run first so i can connect to it
    restart: on-failure


volumes:
  clinic_db_data:

networks:
  clinic_network:
    driver: bridge